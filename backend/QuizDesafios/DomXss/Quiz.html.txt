<section class="catalogo">
  <input [value]="termoBusca()" (input)="atualizarBusca($event)" placeholder="Pesquisar produtos" />  

  <button (click)="buscar()">Buscar</button>

  @if (produtos().length === 0) {
    <p>
      Nenhum produto encontrado para // neutra
      <span [innerHtml]="termoBuscaFormatado()"></span> // correta
    </p>
  }
</section>